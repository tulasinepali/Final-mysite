<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

    <!-- Main Pages -->
    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{% url 'core:home' %}</loc>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
    </url>

    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{% url 'core:about' %}</loc>
        <changefreq>monthly</changefreq>
        <priority>0.5</priority>
    </url>

    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{% url 'core:contact' %}</loc>
        <changefreq>monthly</changefreq>
        <priority>0.5</priority>
    </url>

    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{% url 'core:privacy' %}</loc>
        <changefreq>yearly</changefreq>
        <priority>0.3</priority>
    </url>

    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{% url 'core:terms' %}</loc>
        <changefreq>yearly</changefreq>
        <priority>0.3</priority>
    </url>

    <!-- Section List Pages -->
    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{% url 'notes:list' %}</loc>
        <changefreq>daily</changefreq>
        <priority>0.9</priority>
    </url>

    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{% url 'blog:list' %}</loc>
        <changefreq>daily</changefreq>
        <priority>0.9</priority>
    </url>

    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{% url 'downloads:list' %}</loc>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
    </url>

    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{% url 'quiz:list' %}</loc>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
    </url>

    <!-- Notes -->
    {% for note in all_notes %}
    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{{ note.get_absolute_url }}</loc>
        <lastmod>{{ note.updated_at|date:"Y-m-d" }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
    </url>
    {% endfor %}

    <!-- Blog Posts -->
    {% for post in all_blogs %}
    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{{ post.get_absolute_url }}</loc>
        <lastmod>{{ post.updated_at|date:"Y-m-d" }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.7</priority>
    </url>
    {% endfor %}

    <!-- Downloads -->
    {% for download in all_downloads %}
    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{{ download.get_absolute_url }}</loc>
        <lastmod>{{ download.updated_at|date:"Y-m-d" }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
    </url>
    {% endfor %}

    <!-- Quizzes -->
    {% for quiz in all_quizzes %}
    <url>
        <loc>{{ request.scheme }}://{{ request.get_host }}{{ quiz.get_absolute_url }}</loc>
        <lastmod>{{ quiz.updated_at|date:"Y-m-d" }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.7</priority>
    </url>
    {% endfor %}

</urlset>